[project]
name = "hospital-triage-env"
version = "1.0.0"
description = "Hospital Emergency Room Triage environment for OpenEnv."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic>=2.0.0",
]

[project.scripts]
server = "server:main"
